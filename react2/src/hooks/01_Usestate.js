//USESTATE hook is a function to ADD STATE in functional comp.
//STATE- it is nothing but just values or variables of your comp.
//when we want to create a variable then we can use use-state hook
import React from 'react'

const Usestate1 = () => {
  return (
    <div>counter</div>
    // <button>Increase</button>
  )
}

export default Usestate1